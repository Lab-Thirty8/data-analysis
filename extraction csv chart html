<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.9.1/dist/chart.min.js"></script>
</head>
<body>
    <h2>ONS UK Consumer Prices Index<br>including owner occupiers' housing costs</h2>
    <canvas id="chart" width="800" height="400"></canvas>
    <script>
       //  Data Source: https://www.ons.gov.uk/economy/inflationandpriceindices/timeseries/l55o/mm23
        chartIt();
        async function chartIt() {
            const data = await getData();
            const ctx = document.getElementById('chart').getContext('2d');
            const myChart = new Chart(ctx, {
                type: 'bar',
                data: {
                labels: data.xs,
                datasets: [{
                    label: 'ONS UK CPIH',
                    data: data.ys,
                    backgroundColor: [
                        'rgba(14, 194, 208, 0.5)',
                        'rgba(119, 229, 244, 0.5)',
                        'rgba(207, 251, 252, 0.5)'
                    ],
                    borderColor: ['rgba(14, 194, 208, 0.8)'],
                    borderWidth: 1,
                    fill: true
                }]
            },
            options: {
                scales: {
                    y: {
                        beginAtZero: true
                    }
                }
            }
        });

    };
       async function getData() {
            const xs = [];
            const ys = [];
            const response = await fetch('./CPIH_csv.csv');
            const data = await response.text();
            const table = data.split('\n').slice(9);
            table.forEach(row => {
                const columns = row.split(',');
                const xAxis = columns[0];
                    const date = new Date(xAxis);
                    const xValue = date.getFullYear();
                    const dateString = (xValue);                                   
                xs.push(xValue);
                const yAxis = columns[1];
                    const Text = yAxis.toString();
                    const yValue = Text.slice(1, 4);                  
                ys.push(yValue);                   
                console.log(xAxis, yAxis);
            });
            return { xs, ys }; 
       };
    </script>    
</body>
</html>
